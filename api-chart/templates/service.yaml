# api-chart/templates/service.yaml

apiVersion: v1
kind: Service
metadata:
  name: {{ include "Chart.fullname" . }}
spec:
  selector:
    app: {{ include "Chart.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 5000
  type: {{ .Values.service.type }}
